require "spec_helper"

describe ExtratorrentSearch do
  it "has a version number" do
    expect(ExtratorrentSearch::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
